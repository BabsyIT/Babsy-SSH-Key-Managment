# Inventar für den Rollentest in der CI: die Rolle läuft gegen den Runner selbst.
all:
  children:
    debian_hosts:
      hosts:
        testhost:
          ansible_connection: local
          ansible_become: true
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
